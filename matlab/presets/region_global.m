function region = region_global()
    region.name = 'global';
    region.title_label = 'Global';
    region.result_label = 'Global (all latitudes and longitudes)';
    region.average_label = 'global domain';
    region.lat_bounds = [];
    region.lon_bounds = [];
end